spring:
  mail:
    host: host.host.com
    port: 587
    protocol: smtp
    username: USERNAME
    password: PASSWORD
    properties:
      mail:
        transport:
          protocol: smtp
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          timeout: 8000
